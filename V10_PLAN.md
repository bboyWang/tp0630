# V10 实验方案 & 研究进展汇报

> 汇报日期：2026-07-31
> 项目：青藏高原湿地语义分割 + 主动学习（dp0630 / TPrf0515）
> 作者：XINYU WANG（UCAS）

---

## 一、研究进展总览（v4 → v9）

### 1.1 v4~v6：早期探索（均失败/不稳定，积攒教训）

| 版本 | 尝试了什么 | 结果 | 结论 |
|---|---|---|---|
| v4 | 随机裁剪 512→256、Z-score 归一化 | OA 剧烈波动、不收敛 | Z-score 不适用于稀疏正例标注 |
| v5-v6 | 调参、不同 lr | 不稳定 | 需要更好的数据策略 |

> **核心教训 A**：遥感数据在稀疏正例标注下，Z-score 归一化导致不收敛；**Per-chip Min-Max 归一化才是正解**。

---

### 1.2 v7：四象限策略突破（笔记本 RTX 3050 4GB）

**配置**：U-Net base_ch=32（3,307,907 参数）、bs=2、CE+AMP(fp16)、AdamW lr=1e-3、CosineAnnealingWarmRestarts(T_0=15,T_mult=2)、梯度裁剪 1.0、Per-chip Min-Max 归一化。

**数据**：Train 122 基样本→488 四象限 / Val 20 / Test 300（含弱标签） / Pool 1063。

**结果**：
| 指标 | 成绩 | 备注 |
|---|---|---|
| Best Val OA | **90.41%**（E36） | |
| Best Val mIoU | **35.45%** | |
| Test OA | 70.65% | 含弱标签，分数虚高 |
| Test mIoU | 24.67% | |

**产出**：`uncertain_v7.txt`（1063 pool 排序），据此标注了 ==== 批次 30 个样本。

> **核心教训 B**：**四象限 512→4×256 策略是关键创新**——Val OA 从随机裁剪的 ~60% 跃升至 ~90%。
> **核心教训 C**：Test 集不能混入弱标签——v7 的 Test 分数不可靠，后续版本已修正。

---

### 1.3 v8：基准版本（3050 4GB，建立可对比标杆）

**配置与 v7 相同**（base_ch=32、bs=2、CE+AMP、CosineWarmRestarts），**修正了数据划分**。

**数据**：Train 118 基样本→472 四象限 / Val **48（固定人类标注）** / Test **30（固定人类标注）** / Pool 1308。
划分严格遵循 Niu et al. 2025 ESSD 的 8:2 比例。

**结果**：
| 指标 | 成绩 | 问题 |
|---|---|---|
| Best Val OA | **85.38%**（E10） | OA 震荡 16%~85% |
| Best Val mIoU | **29.68%** | |
| Test OA | **67.07%** | Val-Test 差距 ~18% |
| Test mIoU | **22.28%** | |
| Early Stop | @ E25 | |

**产出**：`uncertain_v8.txt`（1308 pool 排序），据此标注了 v8-1 批次 29 个样本。

> **核心教训 D**：bs=2 梯度噪声大 → OA 疯狂震荡；CosineWarmRestarts 的重启机制也在推波助澜。
> **核心教训 E**：AMP(fp16) 与 Dice Loss 的 inplace 操作冲突 → 只能用 CE，mIoU 因此受限。
> **核心教训 F**：Test OA 67% vs Val OA 85% 差距约 18pt → 泛化差，需更强正则化或更多训练数据。

---

### 1.4 v9：服务器迁移（2×RTX 4090 24GB，本次完成，全面大幅超越 v8）

**硬件解锁**：训练从笔记本 RTX 3050 4GB 迁移至服务器 Dell PowerEdge T640（2× RTX 4090 24GB, CUDA 12.2, 40 核 CPU, 62GB RAM）。

**配置变更（v8 → v9）**：
| 参数 | v8 | v9 | 理由 |
|---|---|---|---|
| base_ch | 32（3.3M） | **64（13.2M）** | 容量解锁 |
| batch size | 2 | **16** | 根治震荡 |
| Loss | 仅 CE | **CE + Dice（1:1）** | 不再被 AMP 限制 |
| 学习率调度 | CosineWarmRestarts | **warmup3→平滑 Cosine** | 去重启，曲线稳定 |
| AMP | fp16+GradScaler | **bf16（无 Scaler）** | Dice 兼容 |
| EMA | 无 | **0.999（双分支评估择优）** | 平滑 + 择优 |
| 训练轮次 | 60ep / ES15 | **100ep / ES20** | 更充裕的收敛空间 |

**数据**：Train 147 基样本→588 四象限（118+29 v8-1）/ Val 48（与 v8 逐样本一致） / Test 30（与 v8 逐样本一致） / Pool 1278。
使用"幻影补位法"精确复现 v8 的 shuffle 索引（01505 缺失导致 ds 1504→1503，插回 phantom 后再做划分），**Val/Test 与 v8 完全相同，指标可直接对比**。

**训练轨迹**（单卡 4090，每轮约 50~65s）：
| 轮次 | Val OA | Val mIoU | 备注 |
|---|---|---|---|
| E1 | 85.88% | 27.30% | 起步即平 v8 最佳 |
| E8 | 89.58% | 37.89% | ▲ Best |
| E11 | 90.60% | 44.33% | ▲ Best |
| E20 | 92.60% | 43.10% | ▲ Best |
| E37 | **92.75%** | **47.76%** | ▲ 最终 Best（raw 分支） |
| E57 | — | — | Early Stop |

**最终成绩对比**（Test 集与 v8 完全相同）：
| 指标 | v8 | v9 | 提升 |
|---|---|---|---|
| Best Val OA | 85.38% | **92.75%** | **+7.4pt** |
| Best Val mIoU | 29.68% | **47.76%** | **+18.1pt** |
| **Test OA** | 67.07% | **92.21%** | **+25.1pt** 🚀 |
| **Test mIoU** | 22.28% | **50.40%** | **+28.1pt** 🚀 |

- 训练曲线**平稳上升，零震荡**（v8 曾在 16%~85% 之间狂跳）——batch=16 和 CE+Dice 的组合拳立竿见影。
- v9 AL Top-30 不确定分数峰值 0.53（v8 为 0.82）——模型整体自信度大幅提高。
- EMA 分支后期剧烈波动始终未胜 raw，best 来自 raw（v10 将调整 EMA 参数）。

**产出**：`uncertain_v9.txt`（1278 pool）、`checkpoints_v9/best.pth`（51.6MB）、`results_v9.txt`。

> **核心教训 G**：数据+训练方法的红利远超架构本身——v9 的最大提升来自 batch 和 loss，而非模型宽度。
> **核心教训 H**：EMA(0.999) 在前期被 init 拖累迟迟不收敛（~E25 才苏醒），后期读数剧烈波动始终未胜 raw——需调整参数或换用 SWA。

---

### 1.5 数据质量管理（并行完成）

- TIFF 波段分析：确认 30 波段中 4 个计数波段（S2CNT_E/L、S1CNT_E/L）被正确丢弃；**NDVI/NDWI/MNDWI/EVI 指数（E+L 双时相共 8 个波段）已预计算在数据中**——无需额外添加指数通道。
- 样本 01505（质量差）已删除；chip_02164 无 JSON（从未进数据集，无影响）。
- 标注批次：原始 116 + --批次 14 + =批次 21 + ==批次 45 + ====批次 30 + **v8-1 批次 29** = 已标注 255（有效 ~225）。

---

## 二、V10 实验方案

### 2.1 改动总览

| # | 改动 | 类型 | 论文创新点 | 预期提升 |
|---|---|---|---|---|
| ① | 背景负标签 | 标签体系升级 | 数据质量基础 | **mIoU +10~20** |
| ② | 90° 旋转增强 | 数据增强 | 遥感定向不变性 | mIoU +1~3 |
| ③ | 3 模型分歧采样 | 主动学习升级 | **核心：集成不确定性** | 间接（v11 收益） |
| ④ | 伪标签半监督 | 半监督 | **核心："人标+机标"闭环** | mIoU +5~15 |

### 2.2 ③+④ 的学术创新链条（论文核心卖点）

③ 和 ④ 不是孤立的——它们共同构成一个**"分歧→人标 / 一致→机标"的闭环**：

```
          Pool 1278 个未标注样本
                   │
      ┌────────────┼────────────┐
      │ 3模型分歧大               │ 3模型一致+高置信
      │ (epistemic uncertainty)   │ (众模型都确定)
      ▼                           ▼
   30个 → 人类标注              N个 → 自动伪标签
   (最大化每一笔标注价值)          (免费吃进训练集)
      │                           │
      └───────────┬───────────────┘
                  ▼
           下一轮训练
```

**学术定位**：这个闭环在"标注极度稀缺（147 人标 vs 1278 未标）"的场景下，将主动学习（最大化人工标注效率）和半监督（自动利用剩余未标注数据）的协同做到极致。集成不确定性的引入，避免了单模型"自信地错"导致伪标签传播错误的经典 failure mode。

适用于：遥感/医学影像等"标注昂贵、未标注海量"的 NLP-surrounding 任务。

---

### 2.3 改动①：背景负标签（标签体系全新升级）

**核心问题**：当前模型仅学"哪是湿地/水体/湖泊"（稀疏正例多边形），从未学"什么不是湿地"（其余=-1 忽略）。这直接导致：
- 虚警率高（背景被错判为湿地/水体），压低少数类 IoU
- mIoU 25%~50% 的瓶颈本质是"没有负类"

**实施方案**：
- 新增第 4 类 **"背景/非湿地"**，LABEL_MAP = {'背景': 0, '湿地': 1, '水体': 2, '湖泊': 3}
- 精选 **30~50 张代表性影像做全要素稠密标注**（每像素归属 4 类之一，无忽略区域）
- 稠密标注样本同时作为**高质量 Val/Test**，指标立刻变得可信
- 训练梯度：稠密标注样本全像素参与 loss，稀疏标注样本仍用 ignore_index=-1 跳过未标区域
- 是一次性标注投入，后续所有版本永久受益

> **为什么是性价比最高的标注投资**：这 30~50 张全要素标注将从根本上解决当前 mIoU 的结构性上限，预期单靠这一项提升 10~20 mIoU。

---

### 2.4 改动②：90° 旋转增强（D4 对称群）

**原理**：遥感正射影像没有"重力方向"——湿地/水体/湖泊的几何形态与朝向无关（rotation-invariant）。现有增强仅覆盖水平/垂直翻转（4 种朝向），加入 90° 旋转后覆盖完整 D4 对称群（8 种朝向）。

**实现**：`_aug` 中新增 `np.rot90(f, k)` 同时旋转特征和 mask（k ∈ {0,1,2,3}，p=0.5 触发）。

> **零成本白送**：约 3 行代码，预期 +1~3 mIoU。

---

### 2.5 改动③：3 模型集成分歧主动采样

**为什么比单模型 Margin 更强**：
- 单模型不确定性（margin）：仅反映模型"类间犹豫"的程度，容易被"自信地错"欺骗。
- 集成不确定性（disagreement）：3 个独立训练的模型在某个样本上**给出不同答案** → 说明训练数据不足以确定该样本 → 这正是最值得标注的样本（epistemic uncertainty）。
- 这是主动学习文献中公认的 gold standard，只是通常因计算成本高昂而不实用——但本项目模型仅 13M 参数，训练约 1 小时/模型，双 4090 完全能负担。

**实施**：seed=42/43/44 训 3 个模型（并行 2 卡约 1h）→ 对 pool 逐像素计算预测分歧度 → 选 Top-30。

> **本身不直接提升 v10 指标**，但让选出的 30 个样本质量远高于单模型 Margin → v11 收益。**对论文的方法部分价值极高**。

---

### 2.6 改动④：伪标签半监督（Mean Teacher）

**核心思路**：1278 个 pool 样本是最大的未利用资产。用训练好的模型给它们预测标签（伪标签），高置信的加入训练，免费扩大有效训练集。

**为什么选 Mean Teacher 架构**：
- EMA teacher（v9 已有 EMA 基础设施，天然继承）
- 相比 FixMatch 等强增强方案，Mean Teacher 更稳定，不需要设计"弱/强"增强策略
- 结合改动③的 3 模型一致性判断：**仅取 3 模型都同意且高置信的像素**作为伪标签——比单 teacher 决策稳健得多

**训练策略**：
- student 总 loss = CE(人工标签) + Dice(人工标签) + λ(t) × [CE(伪标签) + Dice(伪标签)]
- λ(t) 从 0 线性 warmup 到目标值（前期模型不可靠，先学人工标签）
- 伪标签仅在 3 模型 consensus 像素上生效——这是³的协同产物

> **风险预判**：伪标签的确认偏误风险已被改动③的 3 模型共识机制大幅对冲（单模型可能自信地错，3 模型大概率不会同时错同一个像素）。预期 +5~15 mIoU。

---

### 2.7 改动③与④的协同（自动标注 + 人工标注闭环）

改动③负责"选出最值得找人标的 30 张"→ 人类标注后加入训练（高质量保证）。
改动④负责"选出模型们都很确定的 N 张"→ 自动伪标签加入训练（低成本扩充）。

**权衡博弈**：分歧样本 → 人工（贵但准）；共识样本 → 伪标签（免费但有噪声）。二者在 loss 权重上通过 λ(t) 和分歧阈值解耦。

这个"双流不确定性驱动的人机协同标注"框架，在遥感、医学等标注稀缺场景有较强的可迁移性，适合作为论文的 **Method 核心章节**。

---

## 三、标注任务清单（供标注阶段参考）

### 本轮标注目标

| 任务 | 数量 | 标注类型 | 用途 |
|---|---|---|---|
| v9 Top-30 | 30 张 | 稀疏正例多边形（湿地/水体/湖泊） | 常规主动学习，进 Train |
| 背景负标签 | 30~50 张 | **全要素稠密标注**（全班 4 类标满每个像素） | 新 Val/Test + 部分进 Train |

> 全要素稠密标注的 30~50 张建议分配：约 20 张作新 Val、10 张作新 Test（替换/扩充现在的 48/30）、剩余可选进 Train。

### LabelMe 操作提示
- 在 `eiseg_ready\images\` 下打开对应 PNG → 标注多边形 → 保存 JSON（和之前操作完全一致）
- 稠密标注时把**所有**像素至少归为背景/湿地/水体/湖泊之一，不留未标区域
- 启动方式：双击桌面 `启动LabelMe.bat`

---

## 四、代码改动清单（标注完成后执行）

| 文件 | 改动内容 |
|---|---|
| `dataset.py` | LABEL_MAP 加 '背景'=0（其他类顺延）；`_aug` 加 90° 旋转 k-rot90；dense/sparse 混合梯度策略 |
| `model.py` | `num_classes` 3→4 |
| `train_v10.py` | 4 类 CE+Dice+ignore_index；Mean Teacher 双 stream；λ ramp-up schedule；EMA decay 调到 0.9999 |
| `rank_ensemble.py` | 新文件：3 模型分歧打分（替换 margin）；3 模型共识判断（供伪标签用） |
| `ready.txt` | `# v9-1 批次`（v9 Top-30）+ `# v10-neg 批次`（背景负标签） |
| `split_v10.json` | 新的钉死划分（含新 Val/Test） |

---

## 五、硬件与时间预估

| 阶段 | 内容 | 预估时间 |
|---|---|---|
| 标注 | 30（AL）+ 30~50（负标签）= 60~80 张 | 用户自行安排 |
| 代码实现 | 改动清单全部 6 个文件 | 约 30 分钟（自动化） |
| v10 训练 | 单模型 147+→ Train，约 100 epoch | 约 1 小时/模型 |
| 集成 AL | 3 种子训练（可并行）+ 分歧打分 | 约 1.5 小时 |
| v10.1 | 伪标签加入、λ ramp-up 再训 | 约 1~2 小时 |

---

## 六、关键文献参考

1. Niu et al. 2025, ESSD — 方法论参考（青海高原湿地遥感标注与评估框架）
2. Settles 2009, "Active Learning Literature Survey" — Margin Sampling 与 QBC（Query by Committee / 集成分歧）的对比
3. Tarvainen & Valpola 2017, "Mean teachers are better role models" — Mean Teacher 半监督
4. Xu 2006, "Modification of normalised difference water index (MNDWI)" — MNDWI 指数
5. McFeeters 1996, "The use of the Normalized Difference Water Index (NDWI)" — NDWI 指数

---

*文档编制：三楼 AI（opencode） | 2026-07-31*
